sed -i "s/baidu/runoob/g" `grep "baidu" -rl ./`      #当前目录下查找包含 baidu 的字符串，并将字符串 baidu 替换为 runoob

用于设置或显示环境变量。
export [-fnp][变量名称]=[变量设置值]
		-f 　代表[变量名称]中为函数名称。
		-n 　删除指定的变量。变量实际上并未删除，只是不会输出到后续指令的执行环境中。
		-p 　列出所有的shell赋予程序的环境变量。
		
用于转换或删除文件中的字符。
tr [-cdst][--help][--version][第一字符集][第二字符集]  
tr [OPTION]…SET1[SET2] 
-c, --complement：反选设定字符。也就是符合 SET1 的部份不做处理，不符合的剩余部份才进行转换
-d, --delete：删除指令字符
-s, --squeeze-repeats：缩减连续重复的字符成指定的单个字符
-t, --truncate-set1：削减 SET1 指定范围，使之与 SET2 设定长度相等
--help：显示程序用法信息
--version：显示程序本身的版本信息

cat testfile |tr a-z A-Z     # 文件testfile中的小写字母全部转换成大写字母 

"shift 1" 命令用于将 shell 脚本中的参数左移一位。它会把第一个参数从 $1 号位置移除，并将所有其他参数的位置向左移一位

wc指令我们可以计算文件的Byte数、字数、或是列数，若不指定文件名称、或是所给予的文件名为"-"，则wc指令会从标准输入设备读取数据。
wc [-clw][--help][--version][文件...]
-c或--bytes或--chars 只显示Bytes数。
-l或--lines 显示行数。
-w或--words 只显示字数。
--help 在线帮助。
--version 显示版本信息。

>：会覆盖文件中原有的内容，如果目标文件不存在，则会创建文件。
&>：会在原有文件内容末尾追加新内容，如果目标文件不存在，则会创建文件。

lsmod ：查看内核已加载的模块
modinfo：查看模块的基本信息
insmod：向Linux内核中插入一个模块
rmmod 删除内核中的一模块
用法：rmmod [ -f ] [ -w ] [ -s ] [ -v ] [ modulename ]
描述：
    rmmod是一个可以从内核中删除模块的小程序，大多数用户使用modprobe -r去删除模块。

选项：
	-a	删除所有目前不需要的模块
    -v --verbose  显示程序正在做些什么，一般只显示执行时的错误信息。
    -f --force  该选项是非常危险：除非编译内核时，CONFIG_MODULE_FORCE_UNLOAD被设置该命令才有效果，否则没效果。用该选项可以删除正在被使用的模块，设计为不能删除的模块，或者标记为unsafe的模块。
    -w --wait 通常，rmmod拒绝删除正在被使用的模块。使用该选项后，指定的模块会被孤立起来，直到不被使用。
    -s  --syslog  将错误信息写入syslog，而不是标准错误(stderr)。
    -V  --version 版本信息

tftp -g 192.168.1.100 -r hfsplus.ko
netstat -a | grep tftp #Linux查看tftp服务是否开启
netstat -aton | grep tftp #Linux查看tftp服务是否开启

blkid

Busybox中tftp命令的用法
tftp [option] ... host [port]
-g表示下载文件(get)
-r表示远程主机的文件名(remote file)

查找当前目录和所有子目录下的后缀名为.o的文件，删除之
查找：
find . -name "*.o" -exec ls {} \;
删除：
find . -name "*.o" -exec rm -f {} \;

stat a.txt    #查看某个文件的inode信息例

df命令 #查看每个硬盘分区的inode总数和已经使用的数量
ls -i #看到文件名对应的inode号码，当操作对象是一个目录时，列出目录下所有文件的文件名和inode号 -n #查看文件的uid和gid

type cd #判断cd是否是内建命令

echo是一个shell内建命令，用来在终端输出字符串，并在最后默认加上换行符。
echo 命令输出结束后默认会换行，如果不希望换行，可以加上-n参数。 -e 解析以反斜杠\开头的转义字符（默认不会解析）

env #打印当前shell的所有的全局变量


git clean -xfq
	-q, --quiet           be quiet（静默删除，只会告诉你出错信息。如果是成功删除了文件或目录则保持沉默）
    -n, --dry-run         dry run（告诉你会被删除的文件或目录有哪些，删除文件毕竟是一件严肃的事，在执行该命令前请务必加上该参数先确定下）
    -f, --force           force（强制删除，clean.requireForce默认的值是true。这种情况下只有加上该参数才能删除）
    -d                    remove whole directories（删除的范围是整个目录）
    -x                    remove ignored files, too（即使是被添加进gitignore的文件或目录也删掉。这里有坑，下面说~）
    -X                    remove only ignored files（只删除被添加进gitignore的文件或目录）
	-e 					选项允许您指定在 .gitignore 中不应被 Git clean 删除的模式。当您想清除工作目录但希望保留特定文件和目录时，此选项很有用。
	
rm -rf * 删除当前文件夹下所有文件



    #从文件中循环读取每一行
    # while read content                   for line in filename
    # do						or 		   do
    # done < filename                      done
	


如果read接收到的值数量少于预期，则多出的变量为空值，如果数量多于预期，则额外的输入全部保存在最后一个变量中
如果没有为read命令指定变量，则所有的输入全部保存在Shell变量REPLY中
-a array	将输入分配给数组（从索引0开始）。
-d delimiter	将字符串delimter中的第一个字符（而非换行符）作为输入的结束
-e	使用readline处理输入，允许使用和命令行相同的方式编辑输入
-i string	如果用户直接按Enter键，使用string作为默认值，需要配合-e选项使用
-n num	从输入中读取num个字符，而非读取一整行
-p prompt	将字符prompt作为输入提示来显示
-r	原始模式，不将反斜线符解释为转义
-s	静默模式，在用户输入字符时不回显，该模式适用于输入密码或其他机密信息
-t seconds	超时，seconds秒之后终止输入，如果输入超时，read返回非0退出状态值
-u fd	从文件描述符fd中读取输入
原文链接：https://blog.csdn.net/weixin_43770382/article/details/127839581


grep -i：忽略大小写
grep -n：显示匹配的行号
grep -c：只统计匹配的行数
grep -v：取反，你要找的，我不显示
grep -m 数字：多个匹配的话，可以限定显示的行数，匹配几次之后停止。

grep -e：逻辑或，可以匹配多个条件，实现多个选项间的逻辑关系，可以跟多个条件

grep -w：匹配整个单词
grep -E：使用扩展正则表达式 ，也可以用egrep，都是可以使用扩展正则表达式
grep -r :递归目录，不处理软链接



https://blog.csdn.net/m0_72359405/article/details/131786938

shell 字符串拼接

shell 键值对https://blog.csdn.net/u014384314/article/details/82492820

ln -s 软链接(符号链接)


flock https://blog.csdn.net/weixin_30746095/article/details/116879656
当多个进程操作同一份资源时，为了避免损坏数据， 每个进程在运行时都要保证其它进程没有同时操作资源， 这时通过flock命令给资源加锁可以实现此需求。flock - 在打开的文件上应用或删除咨询锁
-s, -share 读锁，共享锁
-x, -e, --exclusive 写锁，独占锁，这是默认值，独占或排他锁，在定向为某文件的FD上设置独占锁而未释放锁的时间内，其他进程试图在定向为此文件的FD上设置共享锁或独占锁都会失败。只要未设置-s参数，此参数默认被设置。
-u, --unlock 解锁，通常不需要解锁，因为在文件关闭时会自动解锁
-n, --nonblock 无法立刻获取锁定时退出，而不是等待
-w设置阻塞超时，当超过设置的秒数，就跳出阻塞，返回1，并继续执行下面语句。
-o必须是使用第一种格式时才可用，表示当执行command前关闭设置锁的FD，以使command的子进程不保持锁。
-c执行其后的comand。

tee命令用于读取标准输入的数据，并将其内容输出成文件。
tee [-ai][--help][--version][文件...]
a或--append 　附加到既有文件的后面，而非覆盖它．
-i或--ignore-interrupts 　忽略中断信号。
--help 　在线帮助。
--version 　显示版本信息。

function pause(){
        read -n 1 -p "$*" INP
        if [[ $INP != '' ]] ; then
            echo -ne '\b \n'
        fi
}

basename 去除文件名的目录部分和后缀部分，返回一个字符串参数的基本文件名称
basename 命令读取 String 参数，删除以 /(斜杠) 结尾的前缀以及任何指定的 Suffix 参数，并将剩余的基本文件名称写至标准输出。basename 命令应用以下创建基本文件名称的规则：
        （1）basename /usr/bin/sort                输出 sort
        （2）basename include/stdio.h .h        输出 stdio
		
内建（builtin）命令caller
返回当前活动的子程序调用的上下文，没有指定expr时，显示当前子程序调用的行号和源文件名
expr是一个非负整数，显示当前子程序调用的行号、子程序名和源文件名

$() 小括号里面是 Linux 命令，作用就是执行里面的命令后返回执行的结果；和 ``（反引号）作用一样
${} 变量替换，大括号里面是变量，取变量的值替换 ${}。一般情况下，$var 与 ${var} 是没有区别的，但是用 ${} 会比较精确的界定变量名称的范围

set -eux

-a 　标示已修改的变量，以供输出至环境变量。
-b 　使被中止的后台程序立刻回报执行状态。
-C 　转向所产生的文件无法覆盖已存在的文件。
-d 　Shell预设会用杂凑表记忆使用过的指令，以加速指令的执行。使用-d参数可取消。
-e 　若指令传回值不等于0，则立即退出shell。
-f　 　取消使用通配符。
-h 　自动记录函数的所在位置。
-H Shell 　可利用"!"加<指令编号>的方式来执行history中记录的指令。
-k 　指令所给的参数都会被视为此指令的环境变量。
-l 　记录for循环的变量名称。
-m 　使用监视模式。
-n 　只读取指令，而不实际执行。
-p 　启动优先顺序模式。
-P 　启动-P参数后，执行指令时，会以实际的文件或目录来取代符号连接。
-t 　执行完随后的指令，即退出shell。
-u 　当执行时使用到未定义过的变量，则显示错误信息。
-v 　显示shell所读取的输入值。
-x 　执行指令后，会先显示该指令及所下的参数。
+<参数> 　取消某个set曾启动的参数。

 ls -l --block-size=m 按M显示文件大小
 只显示目录
 ls -hld */
 
 

rsync -av --exclude='指定文件' 源目录/ 目标目录/
其中，`-a` 表示以归档模式进行同步，保留文件属性和目录结构；`-v` 表示以详细模式输出同步过程；`--exclude` 表示排除指定文件；`源目录/` 表示源目录，最后的斜杠表示同步目录下的所有内容；`目标目录/` 表示目标目录，同样最后的斜杠表示将内容同步到目标目录下。


可以使用 `grep` 命令来查找某个目录下是否存在某个字段。具体命令如下：
grep -r "要查找的字段" /要查找的目录路径


openwrt


例如，假设要在文件 `example.txt` 中查找包含关键字 `example` 的行，并打印前后 3 行，可以使用以下命令：
grep -B 3 -A 3 'example' example.txt


grep -rnsw


去除前后空格
echo str | sed 's/^[[:space:]]*//; s/[[:space:]]*$//'


str="  hello world  "
echo "${str}"         # 输出带有前后空格的字符串
echo "${str#"${str%%[![:space:]]*}"}"  # 去除前面的空格
- `${str}`：这是引用变量 `str` 的值。
- `${str%%[![:space:]]*}`：这部分使用了 Bash 的参数扩展功能。`%%` 是一种模式匹配操作符，它会从字符串的开头开始匹配最长的子串，直到遇到第一个不匹配给定模式的部分为止。这里的模式 `[![:space:]]` 表示任何非空白字符。因此，`${str%%[![:space:]]*}` 会返回 `str` 中从开头到第一个非空白字符之前的那部分字符串。如果 `str` 以非空白字符开始，则这部分结果为空字符串。
- `"${str#"${str%%[![:space:]]*}"}"`：这部分使用了另一种参数扩展形式 `#`，它会从字符串的开头移除与指定模式完全匹配的部分。这里，我们用 `${str%%[![:space:]]*}` 的结果作为模式来移除 `str` 的开头部分。因此，最终的结果就是去除了 `str` 开头所有空白字符后的字符串。




echo "${str%"${str##*[![:space:]]}"}"  # 去除后面的空格

获取文件绝对路径  readlink -f/realpath

ldd 可执行文件 #查看可执行文件依赖的so来源





清空文件内容
1. 使用 `echo` 命令将一个空字符串写入文件：
   ```shell
   echo "" > 文件名
   ```

2. 使用 `cat` 命令结合重定向符号 `>` 来清空文件内容：
   ```shell
   cat /dev/null > 文件名
   ```

3. 使用 `truncate` 命令将文件截断为 0 字节大小：
   ```shell
   truncate -s 0 文件名
   ```

4. 使用 `sed` 命令将所有行删除：
   ```shell
   sed -i '1,$d' 文件名
   ```
   上述命令中，`-i` 表示直接修改文件内容，`1,$` 表示匹配所有行，`d` 表示删除匹配的行。

5. 使用 `echo` 命令结合重定向符号 `>` 和 `tee` 命令将标准输出同时输出到文件和屏幕上：
   ```shell
   echo "" | tee 文件名 >/dev/null
   ```
   
   使用`awk`命令来查找第N个匹配字段的对应行内容
   awk '{for(i=1;i<=NF;i++) if($i=="hello" && ++count==2) {print; break}}' file.txt

  sed -n
  
  
  获取数组元素个数
  ${#array[*]}

可以使用`cp`命令的`-r`选项来递归拷贝目录，但是会连同目录本身一起拷贝。如果只想拷贝目录下的文件，可以使用`-r`选项和`*`通配符来实现：

```
cp -r /path/to/source/* /path/to/destination/
```

这样会将`/path/to/source`目录下的所有文件和子目录拷贝到`/path/to/destination`目录下，但是不会拷贝`/path/to/source`目录本身。

将字符串转换为小写字母
echo ${string,,}
将字符串转换为大写字母：
${string^^} 




ls | grep ${defaultctree} | grep -v ".crc" | xargs -I {} cp {} ${cus_path}

在范围内查找
sed -n '/<other>/,/<\/other>/p'  打印<other><\/other>之间的内容

判断glibc的版本号:  ldd --version
显示glibc的版本号和其他相关信息:  rpm -q glibc


whereis命令：whereis命令可以查找二进制文件、源代码文件和man手册文件的位置，可以使用以下命令查找glibc库文件的位置：
whereis libc.so


grep -e  "asn.*/.ko"##正则 .*匹配任意字符

ldd是一个Linux命令，用于列出一个可执行文件或共享库所依赖的动态链接库。
ld是GNU的链接器，用于将多个目标文件链接成一个可执行文件或共享库。
lld是LLVM的链接器，用于将多个目标文件链接成一个可执行文件或共享库。与ld相比，lld具有更快的链接速度和更低的内存占用。
ld.lld是lld的一个别名，在某些系统中使用。

rsync -a --delete jwy_test/ jwy_109/  快速删除文件夹

  for i in `ls *.rpm`; do rpm -qpl $i|grep "ip6_tables.ko"; if [ $? -eq 0 ]; then echo $i; fi; done
  set -o | grep errexit | grep "off$" >/dev/null && { set -e; NEED_BACKUP_SET_E="true"; }
  
 删除匹配指定字符的行：
sed -i '/指定字符/d' 文件名


unset str
str="" 将字符串设置为空

strip_files+=($item)
strip_files[${#strip_files[@]}]=$item
strip_files=$strip_files" $item"  追加最后一个元素


grep "-o"选项会只显示匹配的字符串，而不包括匹配字符串的行的内容
grep -oE

sed -i '$a//'  '1i//' 在文件尾首追加


要使用sed命令将文件中的某一行删除，可以使用以下命令：

```
sed -i '/CONFIG_CC_CAN_LINK_STATIC=y/d' 文件名
```

sed -n '/pattern/=' filename 使用`sed -i`命令获取某行的行号

要在文件中的特定行后面追加一行，可以使用sed命令的`a`命令。以下是在typedef __builtin_va_list va_list后面追加一行的示例命令：

```shell
sed -i '/typedef __builtin_va_list va_list/a 新行内容' 文件名
```

sed -i '/typedef __builtin_va_list va_list/i // Added comment' filename
解释：

-i：表示直接修改文件。
/typedef __builtin_va_list va_list/：匹配包含 typedef __builtin_va_list va_list 的行。
i：表示在匹配到的行前插入。
// Added comment：表示你要插入的内容，可以根据需要修改。
这条命令会在文件中所有包含 typedef __builtin_va_list va_list 的行之前插入 // Added comment。


要使用 sed 命令在 typedef __builtin_va_list va_list 后面追加一个空行，你可以使用以下命令：
sed -i '/typedef __builtin_va_list va_list/a\\' filename
解释：
-i：表示直接修改文件。
/typedef __builtin_va_list va_list/：匹配包含 typedef __builtin_va_list va_list 的行。
a\：表示在匹配的行后面追加内容。这里的 \ 是必须的，它表示在 a 后面插入一个空行。
filename：是要操作的文件名。

export -n 删除环境变量 
ldconfig -p 打印


awk -F '[ ,	]+' '{print $1, $2}' input.txt  指定多个分隔符分割指定文本


判断编译产物是否开启protector-strong编译选项

 readelf -sW executable | grep stack
 objdump -x executable | grep stack
 
 如果有__stack_chk_guard或者__stack_chk_fail 表示开启， 
 __stack_chk_guard是一个用于栈缓冲区溢出保护的变量。它是GCC（GNU Compiler Collection）编译器在启用 `-fstack-protector` 选项时自动生成的一个全局变量，主要用于检测函数调用过程中栈帧是否被意外地修改或覆盖
 __stack_chk_fail` 是一个由GCC编译器生成的函数，用于处理栈缓冲区溢出的情况。当编译器启用了 `-fstack-protector` 或 `-fstack-protector-all` 选项时，它会在每个需要保护的函数中插入代码来检查栈溢出
 
 当一个函数开始执行时，编译器会将 `__stack_chk_guard` 变量的值保存到栈帧中。当函数即将返回时，编译器会检查这个保存的值是否被篡改。如果发现保存的值与原始值不同，这表明栈可能已经被溢出了，此时就会调用 `__stack_chk_fail` 函数，__stack_chk_fail` 函数的主要作用是报告栈溢出错误，并且通常会导致程序终止运行。这样可以防止恶意攻击者通过栈溢出来执行任意代码，提高了程序的安全性。

 BIND_NOW，确认命令：
readelf -a ./vendor/ufw/arm/dbus-unicom/usr/lib/libdbus-1.so.3| grep BIND

栈保护SP，确认命令：
readelf -Ws ./vendor/ufw/arm/dbus-unicom/usr/bin/dbus-uuidgen | grep stack

RELRO，确认命令：
readelf -a ./vendor/ufw/fttr/dbus-unicom/usr/sbin/dbus-daemon | grep RELRO
 
 
 
判断某个ko、so是否是用thumb2指令集编译的

modinfo *.ko | grep vermagic
readelf -A *.ko | grep Tag_THUMB_ISA_use  # 这个只能显示使用的是thunmb2标准


ps -axf

https://blog.csdn.net/initphp/article/details/43705765
1、autoscan -> configure.scan
2、将configure.scan重命名成configure.ac，修改configure.ac
3、aclocal -> aclocal.m4  将configure.ac 中用到的宏集中到aclocal.m4
4、autoconf  将configure.ac aclocal.m4生成configure脚本
5、autoheader 将用户附加的符号定义acconfig.h  生成configure.h.in
6、创建Makefile.am
7、automake --add-missing  将Makefile.am生成Makefile.in  自动添加一些必要的脚本文件，如果文件不存在，touch创建就行了
8、./configure --prefix= 指定安装路径
9、make && make install

ATE=$APP_TEMPLATE_DIR/llt_apP_template.mk
7VWAP_NULL=
8HW_TTREE_BIN=$HW_VWAP_PLAT_RooT/config/product/common/hw_ttree.bin
9HW_VWAP_PUBLISH=$Hw_VwAP_PLAT_ROOT/publish
10HW_GTEST_BUILD=$HW_ONT_TEST_RooT/gtest/script

cmake 中删掉某个编译选项 remove_compile_options(-O2) 3.16以后支持
3.16以前删掉某个编译选项
set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -Wall -O2 -g")
list(REMOVE_ITEM CMAKE_CXX_FLAGS "-O2")
set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS}")


makefile 中删掉某个编译选项 CFLAGS := $(filter-out -O2, $(CFLAGS)) 

$(filter-out pattern, list) 会从 list 中移除匹配 pattern 的元素。
:= 用于立即赋值，这样修改后的 CFLAGS 会被应用到后续的编译步骤。


1. `RPATH`（可执行文件中指定）
2. `LD_LIBRARY_PATH`（环境变量）
3. `RUNPATH`（可执行文件中指定）
4. `/etc/ld.so.conf` 和 `/etc/ld.so.conf.d/` 中配置的路径（需执行 `ldconfig`）
5. 系统默认路径（如 `/lib`, `/usr/lib` 等）

	 while IFS= read -r line || [[ -n "$line" ]]; do
	 done < file.txt
	 
	 cp -rf 源目录 目标目录  如果目标目录不存在，将源目录拷贝成目标目录，如果存在，拷贝成目标目录下同名的子目录
	 
	 
	 
	 
	 
	 ### ✅ 1. **解压文件**
**语法：**
7z x <压缩包路径> -o<解压目录>

**示例：**
7z x archive.7z -o./output

- `x` 表示解压并保留路径结构（推荐）。
- `-o` 指定输出目录（可选）。
- 如果不加 `-o`，会解压到当前目录。

---

### ✅ 2. **查看压缩包内容（不解压）**
7z l <压缩包路径>

**示例：**
7z l archive.zip

- `l` 表示 list，列出压缩包中所有文件和文件夹。

---

### ✅ 3. **创建压缩包（7z 格式）**
7z a <压缩包名.7z> <文件或目录>

**示例：**
7z a myfiles.7z folder1 file1.txt

- `a` 表示 add，添加文件或目录到压缩包。
- 默认使用 7z 格式。

---

### ✅ 4. **创建 ZIP 压缩包**
7z a -tzip <压缩包名.zip> <文件或目录>

**示例：**
7z a -tzip myfiles.zip file1.txt

- `-tzip` 指定压缩格式为 ZIP。

---

### ✅ 5. **设置压缩级别（0-9，9 最高）**
7z a -mx=9 <压缩包名.7z> <文件或目录>

**示例：**
7z a -mx=9 highcomp.7z data/

- `-mx`：指定压缩级别（0-9），9 是最高压缩率。

---

### ✅ 6. **递归压缩目录**
7z a <压缩包名.7z> <目录名> -r

**示例：**
7z a allfiles.7z project/ -r

- `-r` 表示递归压缩目录内的所有子文件和子目录。

---

### ✅ 7. **测试压缩包完整性**
7z t <压缩包路径>

**示例：**
7z t archive.7z

- `t` 表示 test，检查压缩包是否损坏。

---

### ✅ 8. **删除压缩包中的文件**
7z d <压缩包路径> <要删除的文件名>

**示例：**
7z d archive.7z image.jpg

- `d` 表示 delete。

---

### 常用参数说明：

| 参数 | 说明 |
|------|------|
| `-mx=N` | 设置压缩级别（0-9） |
| `-t格式` | 指定压缩/解压格式，如 `-tzip`、`-ttar` |
| `-o目录` | 指定输出目录 |
| `-r` | 递归处理子目录 |
| `-p密码` | 设置密码（加密压缩） |

---

### 🔒 加密压缩（设置密码）
7z a -p123456 -mhe=on encrypted.7z file.txt

- `-p123456` 设置密码为 `123456`
- `-mhe=on` 启用加密所有文件名（默认只加密内容）


##################################################################################




    local line_num=$(sed -n "/dependency_singletool/=" $upload_yml)
    local vesion_path=$(awk -v line="$line_num" "NR>line {if(/vesion_path/){print;exit}}" $upload_yml | sed "s/'/\"/g" | cut -d'"' -f2)
	
	将line_num传给line，供awk命令使用， 从line之后匹配第一个vesion_path所在的行
	
	
	
	查找第一个匹配到上述字符串的行，并输出行号，使用sed命令
	
	sed -n '/if \[ \$ft\_cpu\_sng\_able \-eq 0 \]\; then/p'  Wistall.sh | head -n 1
	grep  -n -m 1 '/if \[ \$ft\_cpu\_sing\nable \-eq 0 \]\; then/p'  WiInstall.sh | cut -d':' -f1
	
	
	从 insmod $****_sdk_ko_install_path/$_wifi_ko_file_name g_en_rx_packet_4096_length=1 寻找这行后面的第n个fi 并输出行号
	awk -v n="$n" '/insmod \$****_sdk_ko_install_path\/\$****_wifi_ko_file_name g_en_rx_packet_4096_length=1/ {count = 0}  /fi/ {count++}  count == n {print FNR}'   filename.txt
	
	line行之后第一个匹配到  fi的行打印行号
	awk -v line="$line" 'NR >= line && /  fi/ {count++; if (count == 1) {print NR; exit}}' ****/code/config/static_rootfs/bin/WifiPreDfsInstall.sh
	
	
	local start=$(sed -n '/ret = cdk_create_default_tcont_queue_topo();/=' a.c)
    local line=$(awk -v start="$start" 'NR > start&& /return ret;/ {print NR; exit}' a.c)
    sed -i "${line}d" a.c
	
	在某行前面新增一行
	sed -i "/uint32_t max_onu_num = gpst_ptpdrv_dbm->fttr_info->onu_num;/i hello world"        ****/code/source/network/adapter/pon/fwd/ptpdrv/ptpdrv_fttr_np.c
	
	 while IFS= read -r line || [[ -n "$line" ]]; do
	 done < file.txt
	 
	 
	sed -i "s#<\(userName\)>[^<]*</\1>#<\1>${cmc_username_r}</\1>#g" ${****5v2_hgko_path}/ci/cmc/Setting.xml
	 在这段 sed 命令中，括号 () 的作用是用于 捕获组（capture group），以便在替换时引用匹配的内容。

	具体来说：

	<\(userName\)> 中的 \( 和 ) 用于将 userName 捕获为一个组。

	\( 表示开始捕获组。
	userName 是要匹配的内容。
	\) 表示结束捕获组。
	在替换部分 <\1> 中，\1 表示引用第一个捕获组的内容（即 userName）。

	\1 是一个反向引用，表示替换时使用第一个捕获组的内容。
	为什么需要捕获组？
	捕获组的作用是将匹配到的内容保存起来，以便在替换时重复使用。
	在这个命令中，捕获组的作用是确保 <userName> 和 </userName> 标签的名称保持一致。
	如果没有捕获组，替换时无法动态引用标签名称，可能会导致替换错误。
	示例

	comm -23 file1.txt file2.txt
	comm -13 file1.txt file2.txt

	-23` 表示：
  - 不输出文件 2 中独有的行（第二个文件的差集）
  - 输出文件 1 中独有的行（第一个文件的差集）

	-13` 表示：
  - 不输出文件 1中独有的行（第1个文件的差集）
  - 输出文件 2中独有的行（第2个文件的差集）
  
  `comm` 要求两个文件是已排序的。如果文件未排序，可以先用 `sort` 排序：
  
  去除重复行并保留每个重复行的第一次出现
  sort -u file.txt  等价于 sort file.txt | uniq -u
	`-u` 参数表示只输出唯一出现的行。
	
find . -name "*.ko" | awk -F'/' '{print $NF}' | sort -u > 1.txt


find . -name "*.c" -exec grep -v "^$" {} \; | wc -l  统计所有.c文件中的非空行数





alias gitb='git branch'
alias gits='git status'
alias gitms='repo sync -j64 '
alias gitmsd='repo -d --force-sync -j64 -J64'
alias gitcm='git commit --amend'
alias gita="git status | grep modified | cut -d':' -f2 | xargs git add"
alias gitbd='list_b=$(gitb | grep -v "\*"); for branch in $list_b; do repo forall -- git branch -D $branch; done'
alias hist='history | tail'
export PS1="[\t|\d]\u@\w>"
